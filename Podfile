# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Intuitech' do
  use_frameworks!

  # Pods for Intuitech
  pod 'SwiftGen', '~> 6.0'

  target 'IntuitechTests' do
    inherit! :search_paths
    # Pods for testing
    pod 'Nimble'
  end
end
